# شرح قاعدة البيانات - OtakuZone

## 📊 الفرق بين البيانات الموجودة

### 1. **`sample_mflix` Database** (بيانات تجريبية من MongoDB)
هذه **ليست** بيانات مشروع OtakuZone! هذه بيانات تجريبية من MongoDB Atlas تُستخدم للتعلم والاختبار.

**Collections الموجودة:**
- `movies` - أفلام تجريبية
- `theaters` - دور سينما تجريبية
- `users` - مستخدمين تجريبيين (مثل شخصيات Game of Thrones)
- `comments` - تعليقات تجريبية

**⚠️ هذه البيانات لا تُستخدم في مشروع OtakuZone!**

---

### 2. **`otakuzone` Database** (بيانات مشروع OtakuZone)
هذه هي قاعدة البيانات الفعلية لمشروع OtakuZone.

**Collections المطلوبة:**

#### ✅ **Users** (المستخدمون)
- معلومات المستخدمين
- كلمات المرور المشفرة
- الأدوار (user, moderator, admin)
- حالة Premium

#### ✅ **Categories** (التصنيفات)
- نظريات
- شونين
- شوجو
- إيسيكاي
- كوميديا
- رومانس
- أكشن
- دراما
- ساينس فيكشن

#### ✅ **Topics** (المواضيع)
- مواضيع المنتدى
- النوع (أنمي، مانجا، مانهوا)
- التصنيف
- المحتوى
- المؤلف

#### ✅ **Comments** (التعليقات)
- تعليقات على المواضيع
- ردود متداخلة (threaded replies)

#### ✅ **Likes** (الإعجابات)
- إعجابات على المواضيع والتعليقات

#### ✅ **Follows** (المتابعات)
- متابعة المواضيع

#### ✅ **Notifications** (الإشعارات)
- إشعارات للمستخدمين

#### ✅ **Ratings** (التقييمات)
- تقييمات الأنمي

#### ✅ **Anime** (معلومات الأنمي)
- بيانات الأنمي من Jikan API

---

## 🔧 كيفية إنشاء البيانات

### خطوة 1: تحديث ملف `.env`
تأكد من أن `DATABASE_URL` يحتوي على اسم قاعدة البيانات:

```env
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/otakuzone?retryWrites=true&w=majority"
```

**ملاحظة:** اسم قاعدة البيانات `otakuzone` موجود في الرابط.

### خطوة 2: تشغيل Seed Script
```bash
npm run db:seed
```

سيتم إنشاء:
- ✅ 9 تصنيفات (نظريات، شونين، شوجو، إلخ)
- ✅ مستخدم إداري (`admin@otakuzone.com` / `admin123`)
- ✅ مستخدم تجريبي (`user@test.com` / `user123`)
- ✅ مواضيع تجريبية

---

## 📍 أين تجد البيانات في MongoDB Atlas؟

1. افتح MongoDB Atlas
2. اذهب إلى **Data Explorer**
3. في الـ sidebar الأيسر، ابحث عن:
   - **Cluster:** OtakuZone
   - **Database:** `otakuzone` (وليس `sample_mflix`)
   - **Collections:** يجب أن ترى:
     - `users`
     - `categories`
     - `topics`
     - `comments`
     - `likes`
     - `follows`
     - `notifications`
     - `ratings`
     - `anime`

---

## ⚠️ ملاحظات مهمة

1. **`sample_mflix` لا تُستخدم** - هذه بيانات تجريبية فقط
2. **`otakuzone` هي قاعدة البيانات الفعلية** - يجب أن تحتوي على جميع Collections المذكورة أعلاه
3. **إذا لم تجد `otakuzone` database** - قم بتشغيل `npm run db:seed` لإنشائها
4. **إذا كانت Collections فارغة** - قم بتشغيل `npm run db:seed` لملئها بالبيانات الأولية

---

## 🐛 حل المشاكل

### المشكلة: لا أرى `otakuzone` database
**الحل:** قم بتشغيل:
```bash
npm run db:seed
```

### المشكلة: التصنيفات فارغة
**الحل:** تأكد من تشغيل seed script:
```bash
npm run db:seed
```

### المشكلة: لا يمكن اختيار تصنيف عند إنشاء موضوع
**الحل:** 
1. تأكد من وجود `categories` collection في `otakuzone` database
2. تأكد من أن API `/api/categories` يعمل بشكل صحيح
3. تحقق من console في المتصفح للأخطاء

